# ==================================
# API Configuration
# ==================================

# Backend API URL (utilisé par Axios pour les requêtes API)
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# ==================================
# Authentication Configuration
# ==================================

# Better Auth API URL (utilisé pour l'authentification)
# En développement local, pointer vers le backend
NEXT_PUBLIC_BETTER_AUTH_API_URL=http://localhost:8080

# Clé secrète pour le JWT (à remplacer par une clé sécurisée en production)
# Générer avec: openssl rand -base64 32
JWT_SECRET=votre_cle_secrete_a_changer

# ==================================
# Client Configuration
# ==================================

# URL du client frontend (utilisé pour les redirections et callbacks)
# En développement local
NEXT_PUBLIC_CLIENT_URL=http://localhost:3000

# ==================================
# Stripe Configuration
# ==================================

# Les clés Stripe sont gérées côté backend
# Aucune clé Stripe n'est nécessaire côté frontend 